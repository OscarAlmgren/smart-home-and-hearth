# Environment: dev — branch `dev`
#
# dev has NO cluster footprint. It is validated by CI only (kustomize build,
# kubeconform, HA check_config). Nothing is scheduled anywhere. See docs/lcm.md.
#
# New Home Assistant versions land here first.
#
# TRACKED IN GIT — never put secrets in this file.

HA_VERSION=2026.7.4
TZ=Europe/Stockholm
