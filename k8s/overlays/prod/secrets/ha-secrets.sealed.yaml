# PLACEHOLDER — regenerate with ./scripts/seal-secrets.sh before first sync.
#
# Becomes /config/secrets.yaml inside the pod, written by the sync-config
# initContainer. Keys required by config/: see config/secrets.yaml.example.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: ha-secrets
  namespace: ha-prod
spec:
  encryptedData: {}
  template:
    metadata:
      name: ha-secrets
      namespace: ha-prod
    type: Opaque
