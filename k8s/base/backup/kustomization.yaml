apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Nightly restic backup. See docs/disaster-recovery.md for what it captures,
# why, and how to restore.

resources:
  - rbac.yaml
  - cronjob.yaml

configMapGenerator:
  - name: restic-excludes
    files:
      - excludes.txt
