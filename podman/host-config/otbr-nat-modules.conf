# Required by otbr.container: its entrypoint runs its own NAT44 setup via
# legacy iptables and dies if these netfilter tables are not available
# ("Table does not exist (do you need to insmod?)"). Installed by
# scripts/bootstrap-podman.sh into /etc/modules-load.d/.
iptable_nat
iptable_mangle
iptable_filter
ip6table_filter
