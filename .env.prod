# Environment: prod — branch `main`
#
# The only live Home Assistant. Namespace ha-prod on the henrybook node, with
# hostNetwork (mDNS/SSDP discovery) and the Zigbee dongle attached.
# Argo CD auto-syncs this environment.
#
# Only bump HA_VERSION here after the same version has been through dev and
# test. Bumping prod is a production change — see docs/lcm.md § Promotion.
#
# TRACKED IN GIT — never put secrets in this file.

HA_VERSION=2026.7.4
TZ=Europe/Stockholm
